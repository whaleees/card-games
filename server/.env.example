PORT=4000
# Comma-separated list of allowed origins. Use "*" only for quick local testing.
# Set this to your deployed frontend URL once you have one, e.g.:
# CORS_ORIGINS=https://your-app.vercel.app,http://localhost:3000
CORS_ORIGINS=*
